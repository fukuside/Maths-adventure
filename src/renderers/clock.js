export default { kind:"clock", render(question,{escapeHtml}) {
 const hour=Number(question.hour??12),minute=Number(question.minute??0),cx=150,cy=150,radius=112;
 const hourAngle=((hour%12)*30)+minute*.5, minuteAngle=minute*6;
 const numbers=Array.from({length:12},(_,i)=>{const n=i+1,a=((n*30)-90)*Math.PI/180,x=cx+Math.cos(a)*radius,y=cy+Math.sin(a)*radius;return `<text x="${x.toFixed(2)}" y="${y.toFixed(2)}" text-anchor="middle" dominant-baseline="middle" font-size="20" font-weight="700" fill="#172033" style="text-shadow:none;paint-order:normal">${n}</text>`}).join("");
 const ticks=Array.from({length:60},(_,i)=>{const a=(i*6-90)*Math.PI/180,h=i%5===0,o=136,inn=h?124:130;return `<line x1="${(cx+Math.cos(a)*inn).toFixed(2)}" y1="${(cy+Math.sin(a)*inn).toFixed(2)}" x2="${(cx+Math.cos(a)*o).toFixed(2)}" y2="${(cy+Math.sin(a)*o).toFixed(2)}" stroke="#172033" stroke-width="${h?4:2}" stroke-linecap="round"/>`}).join("");
 return `<div class="visual-question clock-question"><svg class="analog-clock-svg" viewBox="0 0 300 300" role="img" aria-label="${hour}時${minute}分のアナログ時計"><defs><radialGradient id="clockFaceGradient" cx="50%" cy="42%" r="62%"><stop offset="0%" stop-color="#fffdf1"/><stop offset="100%" stop-color="#f5dfa1"/></radialGradient><filter id="clockShadow" x="-30%" y="-30%" width="160%" height="160%"><feDropShadow dx="0" dy="8" stdDeviation="8" flood-color="#000" flood-opacity=".32"/></filter></defs><circle cx="150" cy="150" r="143" fill="#d8b86a" filter="url(#clockShadow)"/><circle cx="150" cy="150" r="132" fill="url(#clockFaceGradient)" stroke="#fff7d2" stroke-width="4"/>${ticks}${numbers}<g transform="rotate(${hourAngle} 150 150)"><line x1="150" y1="158" x2="150" y2="88" stroke="#172033" stroke-width="11" stroke-linecap="round"/></g><g transform="rotate(${minuteAngle} 150 150)"><line x1="150" y1="162" x2="150" y2="54" stroke="#243b5a" stroke-width="7" stroke-linecap="round"/></g><circle cx="150" cy="150" r="11" fill="#f7c948" stroke="#172033" stroke-width="5"/></svg><div class="visual-prompt">${escapeHtml(question.prompt??"この とけいは なんじ？")}</div></div>`;
}};
